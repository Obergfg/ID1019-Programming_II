defmodule Freq do 

    def freq(sample) do
      
        freq(sample, ...)
      end
      
      def freq([], freq) do
        
      end
      
      def freq([char | rest], freq) do
        freq(rest, ...)
      end

end